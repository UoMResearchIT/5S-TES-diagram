model {
  extend execution {
    // Components
    funnel = component "TES Engine" "TES compliant job engine" "Funnel, TESK"

    // Relationships
    funnel -> tre.protected_data 'Queries protected data'
  }
}

views {
  view of execution {
    title 'Execution Service'
    include *
  }
}